-- Db2 for z/OS: вычисляемые GENERATED ALWAYS AS (expression) колонки НЕ поддерживаются
-- (ошибка обычно выглядит как “ожидалось CHECK/IDENTITY, а не AS”).
--
-- Поэтому в Test2 вместо generated-колонок используем индексы по выражению SUBSTR(...)
-- и оставляем запрос с SUBSTR (см. 03_create_indexes.sql и script_final.sql).
--
-- Этот файл оставлен как заглушка, чтобы порядок выполнения (01→02→03→04) не ломался.
